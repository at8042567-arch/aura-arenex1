export const SectionTag = ({ children }: { children: React.ReactNode }) => (
  <div className="text-center text-xs uppercase tracking-[0.3em] text-secondary font-semibold mb-3">
    {children}
  </div>
);
